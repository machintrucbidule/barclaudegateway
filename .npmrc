# Refuse to install if the local Node/npm versions don't match "engines" in package.json.
engine-strict=true
# Pin exact dependency versions (no ^ or ~) so installs are reproducible across machines and CI.
save-exact=true
